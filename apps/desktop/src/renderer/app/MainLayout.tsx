import type { App } from '../state/boot.js'

// Stub: the real layout arrives in Task 6. It must accept `app` because
// AppRoot renders `<MainLayout app={app} />`.
export function MainLayout({ app }: { app: App }) {
  return <div>ready</div>
}
